# 需要同步到 ACR 的镜像清单
#
# ⚠️ ACR 个人版最多 3 个仓库，所以所有镜像共用一个仓库，靠 tag 区分。
#
# 格式：
#   <源镜像>                  tag 自动推导：去掉 registry/org 前缀，冒号换成短横线
#   <源镜像>  <自定义tag>      需要自己定 tag 时写第二列
#
# 自动推导示例：
#   mysql:8.4                                → 仓库:mysql-8.4
#   nginx                                    → 仓库:nginx-latest
#   rancher/local-path-provisioner:v0.0.37   → 仓库:local-path-provisioner-v0.0.37
#
# 改完这个文件 push 上去，Actions 会自动同步。# 开头的行和空行会被忽略。

# ⚠️ 跨境推送每个镜像约十几分钟，而 job 超时是 60 分钟。
#    一次别放太多，超过 3~4 个就分批提交。

# ---- 加速器拉不到、必须走这里的 ----
mysql:8.4

# ---- 下面这些先注释，验证通了再分批放开 ----
# eclipse-temurin:21-jdk
# eclipse-temurin:21-jre

# ---- 中间件 ----
# redis:7
# nacos/nacos-server:v2.4.3
# rabbitmq:3.13-management

# ---- k8s 生态 ----
# rancher/local-path-provisioner:v0.0.37

# ---- 示例：自定义 tag ----
# registry.k8s.io/metrics-server/metrics-server:v0.7.2   metrics-server-v0.7.2
