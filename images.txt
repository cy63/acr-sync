# 需要同步到 ACR 的镜像清单
#
# ⚠️ ACR 个人版仓库数量有上限，所有镜像共用一个仓库，靠 tag 区分。
#
# 格式：
#   <源镜像>                 tag 自动推导：去掉 registry/org 前缀，冒号换短横线
#   <源镜像>  <自定义tag>     需要自己定 tag 时写第二列
#
# 推导示例：
#   mysql:8.4                                → 仓库:mysql-8.4
#   apache/rocketmq:5.3.1                    → 仓库:rocketmq-5.3.1
#   docker.elastic.co/.../elasticsearch:8.x  → 仓库:elasticsearch-8.x
#
# 每个镜像是一个独立的并行 job，加多少都不会互相拖慢。
# 改完 push 上去自动触发。# 开头的行和空行会被忽略。

# ---- 数据库 ----
mysql:8.4
mongo:7.0

# ---- 缓存 ----
redis:7.4

# ---- 注册配置中心 ----
# 3.0 线的最新补丁版；v3.2.4 是当前最新稳定版，一并同步以备升级
nacos/nacos-server:v3.0.3
nacos/nacos-server:v3.2.4

# ---- 消息队列 ----
apache/rocketmq:5.3.1
apacherocketmq/rocketmq-dashboard:1.0.0
rabbitmq:3.13-management
# Kafka 用官方 Apache 镜像，自带 KRaft 模式，不需要单独部署 ZooKeeper
apache/kafka:3.9.0

# ---- 流计算 ----
# 项目是 JDK 21，Flink 1.x 没有 java21 镜像，只有 Flink 2.x 有
flink:2.3.0-scala_2.12-java21
# 备选：Flink 1.20 连接器生态更成熟，但运行时是 Java 17
flink:1.20.1-scala_2.12-java17

# ---- 搜索 ----
docker.elastic.co/elasticsearch/elasticsearch:8.15.3

# ---- 限流熔断 ----
bladex/sentinel-dashboard:1.8.8

# ---- 监控 ----
# Spring Boot 侧用 Micrometer 暴露 /actuator/prometheus，这边负责抓取和展示。
# 如果之后上 kube-prometheus-stack（operator + alertmanager + node-exporter +
# kube-state-metrics），那些镜像的版本必须和 Helm chart 对齐，
# 用 helm template 导出实际列表再补进来，不要凭空写版本号。
prom/prometheus:v3.14.0
grafana/grafana:13.2.1

# ---- Java 运行时 ----
eclipse-temurin:21-jdk
eclipse-temurin:21-jre

# ---- CI ----
# 控制器。固定版本而不用 lts-jdk21 浮动标签，避免每次同步都拉到不同镜像
jenkins/jenkins:2.568.3-lts-jdk21
# 动态构建节点，Kubernetes 插件按需拉起。原 tag 太长，第二列自定义一个短的
jenkins/inbound-agent:3391.va_37fa_a_305d6d-1-jdk21   inbound-agent-jdk21
# 构建 Java 项目用。maven 官方镜像就是 Apache Maven，Docker Hub 上没有 apache/maven
# Maven 4 目前仍是 RC（4.0.0-rc-5）。官方镜像没有 3.8.9，3.8 线最新是 3.8.8
# 且无 temurin-21 变体，故用 3.9.9（对 3.8 的构建向后兼容）
maven:3.9.9-eclipse-temurin-21
# 在 k8s 里构建镜像。containerd 没有 docker socket，
# kaniko 已停止维护，用 buildkit
moby/buildkit:v0.18.2-rootless

# ---- 可选，需要时取消注释 ----
# docker.elastic.co/kibana/kibana:8.15.3
# rancher/local-path-provisioner:v0.0.37
