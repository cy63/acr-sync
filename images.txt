# 需要同步到 ACR 的镜像清单
#
# ⚠️ ACR 个人版仓库数量有上限，所有镜像共用一个仓库，靠 tag 区分。
#
# 格式：
#   <源镜像>                 tag 自动推导：去掉 registry/org 前缀，冒号换短横线
#   <源镜像>  <自定义tag>     需要自己定 tag 时写第二列
#
# 推导示例：
#   mysql:8.4                                → 仓库:mysql-8.4
#   apache/rocketmq:5.3.1                    → 仓库:rocketmq-5.3.1
#   docker.elastic.co/.../elasticsearch:8.x  → 仓库:elasticsearch-8.x
#
# 每个镜像是一个独立的并行 job，加多少都不会互相拖慢。
# 改完 push 上去自动触发。# 开头的行和空行会被忽略。

# ---- 数据库 ----
mysql:8.4
mongo:7.0

# ---- 缓存 ----
redis:7.4

# ---- 注册配置中心 ----
# 3.0 线的最新补丁版；v3.2.4 是当前最新稳定版，一并同步以备升级
nacos/nacos-server:v3.0.3
nacos/nacos-server:v3.2.4

# ---- 消息队列 ----
apache/rocketmq:5.3.1
apacherocketmq/rocketmq-dashboard:1.0.0
rabbitmq:3.13-management
# Kafka 用官方 Apache 镜像，自带 KRaft 模式，不需要单独部署 ZooKeeper
apache/kafka:3.9.0

# ---- 流计算 ----
# 项目是 JDK 21，Flink 1.x 没有 java21 镜像，只有 Flink 2.x 有
flink:2.3.0-scala_2.12-java21
# 备选：Flink 1.20 连接器生态更成熟，但运行时是 Java 17
flink:1.20.1-scala_2.12-java17

# ---- 私有镜像仓库 ----
# 自建 registry，给 Jenkins 构建的应用镜像用。
# 为什么不推 ACR：实测家宽上行只有 4.73 Mbit/s（约 590 KB/s），
# 一个 300MB 的 Spring Boot 镜像推上去要 8.5 分钟；推局域网只要几秒。
# ⚠️ registry 3.x 是 CNCF distribution，配置文件路径从 2.x 的
#    /etc/docker/registry/config.yml 改成了 /etc/distribution/config.yml
registry:3.1.1

# ---- 对象存储 ----
# Flink checkpoint 需要「所有 TaskManager 和 JobManager 都能访问的共享存储」，
# 而 local-path 是节点本地的 ReadWriteOnce，做不到。MinIO 提供 S3 兼容端点补上这块。
# ⚠️ 社区版已移除 Web 控制台的管理功能，只剩对象浏览器；
#    建桶、建账号、配策略都要用 mc 命令行
minio/minio:RELEASE.2025-09-07T16-13-09Z    minio-2025-09-07
# mc 是 MinIO 的命令行客户端，用一次性 Job 跑来建桶。
# 这里用 latest 是例外 —— 它只在初始化时跑一次，不常驻，版本漂移无影响
minio/mc:latest

# ---- 搜索 ----
docker.elastic.co/elasticsearch/elasticsearch:8.15.3
# ES 的查询界面。SkyWalking UI 只能按 service + 时间 + 关键字粗查，
# 全文检索、聚合分析、做 dashboard 还是要 Kibana。
# ⚠️ 版本必须和 ES 严格对齐（8.15.3 对 8.15.3）——
#    Kibana 拒绝连接大版本不一致的 ES，小版本超前也会告警。
docker.elastic.co/kibana/kibana:8.15.3
# 日志采集。以 DaemonSet 跑在每个节点上，读 /var/log/pods 里的容器 stdout。
# ⚠️ 业务服务【不需要改任何配置】—— 那个目录是 kubelet 自动落盘的。
# ⚠️ 选 Filebeat 而不是更轻的 Fluent Bit，是因为它和 ES/Kibana 同厂同版本：
#    自带 MySQL / Redis / Kafka / Nginx 的解析 module 和现成的 Kibana 仪表盘，
#    而 Fluent Bit 每种中间件都要自己写正则。内存差 100MB × 3 节点，值。
# ⚠️ 版本同样要和 ES 对齐。
docker.elastic.co/beats/filebeat:8.15.3

# ---- 限流熔断 ----
bladex/sentinel-dashboard:1.8.8

# ---- 监控 ----
# Spring Boot 侧用 Micrometer 暴露 /actuator/prometheus，这边负责抓取和展示。
# 采用「独立 Prometheus + kubernetes_sd_configs」而不是 kube-prometheus-stack：
# 后者要额外同步 6 个镜像且版本必须和 Helm chart 严格对齐，
# 而 Prometheus 原生的 k8s 服务发现已经够用，配置也更透明。
prom/prometheus:v3.14.0
grafana/grafana:13.2.1
# 节点级指标：CPU / 内存 / 磁盘 / 网络。以 DaemonSet 跑在每个节点上。
# ⚠️ 「磁盘剩余空间」这个指标只有它能提供 —— 而这正是本项目最需要的告警：
#    local-path 不强制容量，磁盘写满会让节点 NotReady
prom/node-exporter:v1.12.1
# k8s 对象状态：Pod 重启次数、Deployment 副本数、PVC 容量、节点 Ready 状态
registry.k8s.io/kube-state-metrics/kube-state-metrics:v2.20.0

# ---- 链路追踪（SkyWalking）----
# OAP 是后端（接收 + 分析 + 存储），UI 是前端，两个是分开的镜像。
# ⚠️ 存储用现有的 ES 8.15.3 —— 官方支持 ES 7.x / 8.x / 9.x。
#    OAP 10.x 的默认存储已经改成 BanyanDB，用 ES 要显式设 SW_STORAGE=elasticsearch。
apache/skywalking-oap-server:10.1.0
apache/skywalking-ui:10.1.0
# Java 探针。⚠️ 这个镜像【不是拿来跑服务的】—— 它只是个装着 agent jar 的容器，
#    用法是在业务 Pod 里当 initContainer，把 /skywalking/agent 拷到 emptyDir，
#    业务容器挂载同一个 emptyDir 并用 -javaagent 加载。
#    这样【16 个业务镜像一个都不用重建】。
# ⚠️ 必须选 java21 变体 —— 业务镜像是 eclipse-temurin-21-jre，
#    agent 的字节码增强要和目标 JVM 版本匹配。
# ⚠️ agent 版本号（9.6.0）和 OAP 版本号（10.1.0）【不是一套】，
#    SkyWalking 的 Java agent 是独立仓库独立发版，别看到对不上就以为拿错了。
apache/skywalking-java-agent:9.6.0-java21

# ---- Java 运行时 ----
eclipse-temurin:21-jdk
eclipse-temurin:21-jre

# ---- CI ----
# 控制器。固定版本而不用 lts-jdk21 浮动标签，避免每次同步都拉到不同镜像
jenkins/jenkins:2.568.3-lts-jdk21
# 动态构建节点，Kubernetes 插件按需拉起。原 tag 太长，第二列自定义一个短的
jenkins/inbound-agent:3391.va_37fa_a_305d6d-1-jdk21   inbound-agent-jdk21
# 构建 Java 项目用。maven 官方镜像就是 Apache Maven，Docker Hub 上没有 apache/maven
# Maven 4 目前仍是 RC（4.0.0-rc-5）。官方镜像没有 3.8.9，3.8 线最新是 3.8.8
# 且无 temurin-21 变体，故用 3.9.9（对 3.8 的构建向后兼容）
maven:3.9.9-eclipse-temurin-21
# 在 k8s 里构建镜像。containerd 没有 docker socket，
# kaniko 已停止维护，用 buildkit
moby/buildkit:v0.18.2-rootless

# ---- 可选，需要时取消注释 ----
# rancher/local-path-provisioner:v0.0.37

# ---- 部署工具 ----
# Jenkins 流水线里执行 kubectl set image / rollout status。
# ⚠️ 不能用 registry.k8s.io/kubectl —— 那是 distroless 镜像，没有 shell，
#    Jenkins 的 Kubernetes 插件需要往容器里注入命令，容器必须能跑 sh 且常驻
alpine/kubectl:1.36.4
